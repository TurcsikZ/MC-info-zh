function paralelogramWithEdges(magassag, oldal) {
    var terulet = magassag * oldal;
    {
        return terulet;
    }
    ;
}
console.log(paralelogramWithEdges(10,5))

function paralelogramWithAngles(oldal1, oldal2, szog) {
    var terulet = oldal1 * oldal2 * Math.sin(szog);
    {
        return terulet;
    }
}
console.log(paralelogramWithAngles(10,5,90))