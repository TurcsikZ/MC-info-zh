function paralelogramWithEdges(magassag: number, oldal: number): number {
    let terulet = magassag * oldal;
{return terulet};
}

function paralelogramWithAngles(oldal1: number, oldal2: number, szog:number): number {
    let terulet = oldal1 * oldal2 * Math.sin(szog);
    {return terulet}
}